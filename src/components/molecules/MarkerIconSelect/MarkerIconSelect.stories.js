import MarkerIconSelect from './MarkerIconSelect'

const Template = (args) => <MarkerIconSelect {...args} />

export const Default = Template.bind({})
Default.args = {}

export default {
  title: 'molecules/MarkerIconSelect',
  component: MarkerIconSelect,
}
