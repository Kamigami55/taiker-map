import MapStyleSelect from './MapStyleSelect'

const Template = (args) => <MapStyleSelect {...args} />

export const Default = Template.bind({})
Default.args = {}

export default {
  title: 'molecules/MapStyleSelect',
  component: MapStyleSelect,
}
