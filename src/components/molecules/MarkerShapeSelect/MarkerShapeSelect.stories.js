import MarkerShapeSelect from './MarkerShapeSelect'

const Template = (args) => <MarkerShapeSelect {...args} />

export const Default = Template.bind({})
Default.args = {}

export default {
  title: 'molecules/MarkerShapeSelect',
  component: MarkerShapeSelect,
}
