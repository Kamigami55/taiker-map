import StylePanel from './StylePanel'

const Template = (args) => <StylePanel {...args} />

export const Default = Template.bind({})

export default {
  title: 'organisms/StylePanel',
  component: StylePanel,
}
