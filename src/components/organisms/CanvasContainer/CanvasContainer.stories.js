import CanvasContainer from './CanvasContainer'

const Template = (args) => <CanvasContainer {...args} />

export const Default = Template.bind({})

export default {
  title: 'organisms/CanvasContainer',
  component: CanvasContainer,
}
